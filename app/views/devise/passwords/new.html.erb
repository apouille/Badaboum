<div class = "bg-sign">
    <div class= "container">
        <div class="row d-flex justify-content-center align-items-center", style = "height: 800px">
            <div class="col-4", style = "min-width: 300px">
                <div class= "form-control border">
                    <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "form-signin p-2" }) do |f| %>
                        <h1 class="h3 mb-3 font-weight-normal text-center text-primary">Password oublié</h1>
                        <%= f.error_notification %>
                        <div class="form-group">
                            <%= f.label :email, "Email", class: "text-primary" %>
                            <%= f.email_field :email,
                                            required: true,
                                            autofocus: true,
                                            input_html: { autocomplete: "email" }, 
                                            class: "form-control" %>
                        </div>
                        <div class="actions">
                            <%= f.submit "Envoyer le lien", class: "btn btn-lg btn-primary btn-block" %>
                        </div>
                    <% end %>
                    <%= render "devise/shared/links" %>
                </div>
            </div>
        </div>
    </div>
</di
