<h3 class="heading heading-6 strong-600 text-uppercase mb-0"> Mes réservations annulées </h3>
  
  <div class="row mb-3 mt-3">
    <div class="card col-12 ">
            <table class="table table-hover mb-0">
              <thead>
                <tr>
                  <th>Commande #</th>
                  <th>Photo</th>
                  <th>Produit</th>
                  <th>Date d'achat</th>
                  <th>Vendeur</th>
                  <th>Statut</th>
                  <th>Montant</th>
                </tr>
              </thead>
              <tbody>
                <% @disputed_orders.each do |disputed_order|%>
                <tr>
                  <td><a class="navi-link" data-toggle="modal"><%=disputed_order.id%></a></td>
                  <td class="product-image">  <% if disputed_order.product.pictures.attached? %>
                        <%= image_tag disputed_order.product?pictures.first, alt:disputed_order.product.title %>
                        <% else %>
                        <%= image_tag("shoes.jpg", alt:disputed_order.product.title,size: "50") %>
                        <% end %></td>
                  <td><%=disputed_order.product.title%></td>
                  <td><%=disputed_order.updated_at.strftime("%Y-%m-%d") %></td>
                  <td><%=disputed_order.product.seller.profile.first_name%></td>
                  
                  <%if disputed_order.status=='disputed'%>
                  <td><span class="badge badge-danger m-0">En cours</span></td>
                  <%else%>
                   <td><span class="badge badge-success m-0"> Remboursée</span></td>
                <%end%>
                 
                
                  <td><%=disputed_order.product.price%> €</td>
                </tr>
                <%end%>
              </tbody>
            </table>
        </div>  
  </div>